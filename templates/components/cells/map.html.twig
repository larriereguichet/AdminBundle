{% trans_default_domain translationDomain %}
<span {{ attributes.defaults({class: 'badge bg-secondary'}) }}>
    {% if mappedValue %}
        {% if translatable %}{{ text|trans }}{% else %}{{ mappedValue }}{% endif %}
    {% else %}
        {{ data }}
    {% endif %}
</span>
