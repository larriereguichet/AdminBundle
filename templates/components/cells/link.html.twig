{% trans_default_domain translationDomain %}
{% if url %}
<a href="{{ url }}" {{ attributes }}>
    {% if icon %}<i {{ iconAttributes.default('bi') }}></i>{% endif %}

    {% if translation %}
        {{ text|trans(translationParameters) }}
    {% else %}
        {{ text }}
    {% endif %}
</a>
{% endif %}
