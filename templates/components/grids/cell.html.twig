{% if cell.template %}
    {% include cell.template with {
        'cell': cell,
        'options': cell.options,
        'context': cell.context,
        'data': cell.data,
        'attributes': attributes,
    } %}
{% endif %}