<p {{ attributes }}>
    <span class="date">{{ data|format_datetime(property.dateFormat, 'none') }}</span>

    {% if property.timeFormat != 'none' %}
        <span class="time">{{ data|format_datetime('none', property.timeFormat) }}</span>
    {% endif %}
</p>
