{% trans_default_domain property.translationDomain %}
{% set data = property.map[data]|default('') %}

<span {{ attributes.defaults({class: 'badge bg-secondary'}) }}>
    {% if property.translatable %}{{ data|trans }}{% else %}{{ data }}{% endif %}
</span>
