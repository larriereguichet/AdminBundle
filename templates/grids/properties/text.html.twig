<p {{ attributes }}>
    {% if property.prefix %}{{ property.prefix }}{% endif %}
    {{ data|default(property.empty)|u.truncate(property.length, property.replace)|striptags }}
    {% if property.suffix %}{{ property.suffix }}{% endif %}
</p>
