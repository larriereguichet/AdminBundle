{% macro attributes(attribute_list, required = [], defaults = []) %}
{%- for attribute, attribute_value in lag_admin_merge_attributes(attribute_list, required, defaults) -%}
    {{- " " -}}
    {{ attribute }}="{{ attribute_value|trim }}"
{%- endfor -%}
{% endmacro %}
